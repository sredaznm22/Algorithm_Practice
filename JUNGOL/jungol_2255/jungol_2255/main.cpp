#include <cstdio>

using namespace std;

int n,arr[20010],copy[20010], rep=0,mix[20010];

void find()
{
    int temp;
    if(rep%2==0)
        for(temp=0;temp<n;temp++) copy[temp]=
    else
        for(temp=0.temp<n;temp++)
            
    while(1)
    {
        for
    }
}

int main()
{
    int temp;
    scanf("%d",&n);
    for(temp=0;temp<n;temp++) scanf("%d",mix[temp]);
//    find();
    printf("%d",n);
    
}
8
